[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend/tg_bot && npm ci"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "cd backend/tg_bot && npm start"

